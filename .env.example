# No server-side secrets are required for the current lead-capture flow.
# /api/resource-lead emits structured Vercel runtime events.
# The hourly ChatGPT automation syncs those events into the canonical Google Sheet.
